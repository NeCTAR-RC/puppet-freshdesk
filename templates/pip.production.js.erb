module.exports = {
  /***************************************************************************
   * Set the default database connection for models in the development       *
   * environment (see config/connections.js and config/models.js )           *
   ***************************************************************************/
  auth: {
    jwsSecret: "<%= @jws_secret %>",
    algo: "HS256",
    aaf: {
      login_url: "<%= @aaf_login_url %>",
    },
    freshdesk: {
      url: "<%= @freshdesk_url %>",
      key: "<%= @freshdesk_key %>",
    }
  },
<% if @port -%>
  port: "<%= @port %>",
<% end -%>
};
